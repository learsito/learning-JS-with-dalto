/* Operadores en JS */
  //Operadores aritméticos: Toman valores numéricos (ya sean literales o variables) como sus operandos y retornan un valor numérico único.
  + //Suma
  -- //Decremento
  / // División
  ** //exponenciación
  ++ //Incremento
  * //multiplicación
  % // Resto (reminder)
  - //Substracción (Resta)
  - // (Unary negation) Negación, se coloca antes
  + // (Unary plus) , se coloca antes

  //Operadores de asignación: Asignan un valor al operando de la izquierda basado en el valor del operando de la derecha.
  x = y // x = y -- Asignación
  x += y // x = x + y -- '' de adicción
  x -= y // x = x - y -- '' de sustracción
  x *= y // x = x * y -- '' de multiplicación
  x /= y // x = x / y -- '' de división
  x %= y // x = x % y -- '' de Resto, es (x / y) y nos muestra el restante como numero entero.
  x **= y // x = x ** y -- '' de exponenciación, es x elevado a y
  x <<= y // x = x << y -- '' de desplazamiento a la izquierda *A partir de aquí son operadores más complejos y no se van a tomar en cuenta, prácticamente no se usan excepto para cosas muy concretas*
  x >>= y // x = x >> y -- '' de desplazamiento a la derecha
  x >>>= y // x = x >>> y -- '' sin signo de desplazamiento a la derecha
  x &= y // x = x & y -- '' AND
  x ^= y // x = x ^ y -- '' XOR
  x |= y // x = x | y -- '' OR
